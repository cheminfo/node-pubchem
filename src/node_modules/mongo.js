'use strict';

const config = require('config');
const MongoClient = require('mongodb').MongoClient;

exports.connect = function () {
    return MongoClient.connect(config.mongodb, {autoReconnect: true});
};
